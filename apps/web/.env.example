# Copy to apps/web/.env.local and fill in from the Supabase project settings.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
# Multiplayer WebSocket (local: ws://localhost:8787 — /ws is appended if missing)
VITE_WS_URL=ws://localhost:8787
